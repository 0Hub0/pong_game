<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:defaultValue="Easy"
        android:entries="@array/pref_difficulty_choices"
        android:entryValues="@array/pref_example_list_values"
        android:key="difficulty"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_title_difficulty" />

    <ListPreference
        android:defaultValue="White"
        android:entries="@array/pref_color_choices"
        android:entryValues="@array/pref_example_color_list_values"
        android:key="color_pong"
        android:title="@string/pref_title_pong_color"
        />

    <EditTextPreference
        android:defaultValue="10"
        android:key="score_max"
        android:selectAllOnFocus="true"
        android:singleLine="true"
        android:title="@string/pref_max_score" />



</PreferenceScreen>
